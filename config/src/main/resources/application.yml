spring:
  profile:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/meelas/kafka-ms.git/
          search-paths:
            - "EmailServiceConfigProperties/{application}/{profile}"
